<?xml version="1.0" encoding="utf-8"?>
<!-- Background con efecto ripple sutil y diseño de tarjeta iOS -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Ripple más suave, no tan invasivo -->
    
    <!-- Máscara que define los bordes del efecto ripple -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="20dp" /> <!-- Radio Premium -->
        </shape>
    </item>
    
    <!-- Fondo real de la tarjeta de la meta -->
    <item>
        <shape android:shape="rectangle">
            <!-- Superficie elevada para que contraste con el fondo del widget -->
            <solid android:color="?attr/colorSurfaceContainerHigh" /> 
            <corners android:radius="20dp" />
        </shape>
    </item>
</ripple>