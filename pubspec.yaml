# Nombre interno del paquete, actualizado a la convención recomendada.
name: sasper
description: "Una aplicación para finanzas personales."
publish_to: 'none' # Evita que se publique accidentalmente.

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

# Dependencias que la aplicación necesita para funcionar.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # <-- AÑADE ESTA LÍNEA
    sdk: flutter 
  

  # Contactos
  flutter_contacts: ^1.1.9


  # Porcentaje
  percent_indicator: ^4.2.3 # O la última versión disponible
  # Iconos de estilo iOS
  cupertino_icons: ^1.0.8
  line_awesome_flutter: ^3.0.1 # Actualizado

  # Base de datos y backend
  supabase_flutter: ^2.8.0 # Actualizado

  # Herramientas de formato y utilidades
  intl: ^0.20.1 # Actualizado
  http: ^1.2.2 # Actualizado
  app_links: ^6.4.0 # Actualizado
  permission_handler: ^12.0.1 # Actualizado

  #Passwords
  flutter_dotenv: ^6.0.0 # Actualizado
  
  # UI y Gráficos
  fl_chart: ^1.0.0 # Actualizado
  google_fonts: ^6.3.0 # Actualizado
  iconsax: ^0.0.8
  path_provider: ^2.1.5 # Actualizado
  
  # Widgets y efectos visuales
  shimmer: ^3.0.0
  flutter_staggered_animations: ^1.1.1
  flutter_heatmap_calendar: ^1.0.5
  flutter_svg: ^2.0.17 # Actualizado
  flutter_animate: ^4.5.2 # Actualizado
  lottie: ^3.2.0 # Actualizado
  skeletonizer: ^2.1.0+1 # Actualizado
  confetti: ^0.8.0 
  animations: ^2.0.11
  quick_actions: ^1.1.0
  
  # Theming y colores dinámicos
  dynamic_color: ^1.8.0 # Actualizado
  flutter_iconpicker: ^4.0.1 # Para seleccionar iconos fácilmente
  flutter_colorpicker: ^1.1.0 # Actualizado
  #uni_links: ^0.5.1

  # Widget de la pantalla de inicio
  home_widget: ^0.8.0 # Actualizado
  
  # Para mostrar texto con formato (Markdown)
  #flutter_markdown: ^0.7.4 # Actualizado
  flutter_markdown_plus: ^1.0.5
  
  # Dependencias para Notificaciones Push
  firebase_core: ^4.2.1 # Actualizado
  firebase_messaging: ^16.0.4 # Actualizado
  connectivity_plus: ^7.0.0 # Actualizado
  equatable: ^2.0.7 # Actualizado
  provider: ^6.1.5 # Actualizado
  shared_preferences: ^2.5.3 # Actualizado
  flutter_local_notifications: ^19.4.0 # Actualizado
  timezone: ^0.10.0
  flutter_timezone: ^5.0.1 # Actualizado

  # Seguridad 
  local_auth: ^3.0.0 

  # Imagenes generadas
  screenshot: ^3.0.0  # Para capturar widgets como imágenes
  share_plus: ^12.0.1   # Para abrir el diálogo de compartir nativo
  image_picker: ^1.2.0
  
  # Geolocalización
  google_places_flutter: ^2.1.1
  uuid: ^4.5.1
  geolocator: ^14.0.2
  
  flutter_riverpod: ^3.0.0
  crypto: ^3.0.6
  url_launcher: ^6.3.2
  flutter_quill: ^11.5.0
  
# Dependencias que solo se usan durante el desarrollo.
dev_dependencies:
  flutter_test:
    sdk: flutter

  # Paquete para generar los íconos de la aplicación.
  flutter_launcher_icons: ^0.14.4 # Actualizado

  # Herramientas para asegurar un código limpio y consistente.
  flutter_lints: ^6.0.0

# Configuración específica de Flutter.
flutter:
  uses-material-design: true

  # ¡IMPORTANTE! Declara la carpeta de assets para que Flutter la incluya.
  assets:
    - assets/icons/
    - assets/animations/
    - .env
    - assets/Images/LaCondicionHumana.jpg

  # Shaders para efectos visuales (como el ruido en la navbar).
  shaders:
    - shaders/noise.frag

# --- CONFIGURACIÓN PARA EL GENERADOR DE ÍCONOS ---
# Esta sección es leída por el paquete flutter_launcher_icons.
flutter_launcher_icons:
  # Activa la generación de íconos para ambas plataformas.
  android: true
  ios: true
  
  # Ruta a tu ícono principal (cuadrado, con fondo).
  # Se usará para iOS y versiones antiguas de Android.
  image_path: "assets/icons/SasPer.png"
  
  min_sdk_android: 21
  
  # --- CONFIGURACIÓN PARA ÍCONOS ADAPTATIVOS (Android 8.0+) ---
  # Color que se usará como capa de fondo.
  adaptive_icon_background: "#2a2a2a" 
  
  # Ruta a tu ícono de primer plano (sin fondo, solo el logo).
  adaptive_icon_foreground: "assets/icons/SasPer_foreground.png"